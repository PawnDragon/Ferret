program: scripts/fedsubmuon_sweep.sh
project: ferret
method: bayes
metric:
  name: eval/loss
  goal: minimize
parameters:
  lr:
    distribution: log_uniform_values
    min: 1e-5
    max: 1e-2
  rank_r:
    values: [4, 8, 16, 32, 64]
command:
  - ${env}
  - bash
  - ${program}
  - ${args}
