program: scripts/fedsubmuon_sweep.sh
project: ferret
method: grid
metric:
  name: eval/loss
  goal: minimize
parameters:
  lr:
    values: [0.01, 0.005, 0.001, 0.0001]
  rank_r:
    values: [4, 8, 16, 32, 64]
command:
  - ${env}
  - bash
  - ${program}
  - ${args}
